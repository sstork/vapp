Building
========================================================================
Set the PIN_HOME variable and execute the make command on the top directory. 

Alternatively you can specify at the PIN_HOME variable at the command line : make PIN_HOME=/path/to/pin.
